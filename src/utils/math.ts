export const add = (a: number, b: number): number => {
  return a + b;
};

export const substract = (a: number, b: number): number => {
  return a - b;
};

export function zeroCheck(num: number): boolean {
  return num == 0;
}
